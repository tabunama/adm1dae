Installation
============

Install from PyPI:

.. code-block:: bash

   pip install adm1dae

Optional plotting support:

.. code-block:: bash

   pip install "adm1dae[plot]"

Notes
-----

The currently published artifact is a compiled Windows x64 / CPython 3.12 wheel.
